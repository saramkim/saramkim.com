nodeLinker: node-modules

# ESLint 9 still requires minimatch 3, whose brace-expansion 1.x is included in
# GHSA-mh99-v99m-4gvg. Forcing brace-expansion 5 breaks minimatch at runtime.
# This dev-only path receives repository-controlled glob patterns.
npmAuditIgnoreAdvisories:
  - 1124334
